{
  "name": "esm-loaders",
  "version": "0.1.0",
  "description": "Composable Node.js ESM '--experimental-loader's",
  "author": "Concision <contact@concision.me>",
  "license": "MIT",
  "repository": "https://github.com/concision/esm-loaders.git",
  "private": true,
  "type": "module",
  "workspaces": [
    "loader-types",
    "chain-polyfill",
    "http",
    "typescript",
    "coffeescript"
  ],
  "scripts": {
    "clean": "yarn workspaces foreach --verbose run clean",
    "lint": "yarn workspaces foreach --verbose run clean",
    "build": "yarn workspaces foreach --verbose run build",
    "prepack": "yarn workspaces foreach --verbose run prepack"
  }
}
