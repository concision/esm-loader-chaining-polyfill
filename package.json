{
  "name": "esm-loaders",
  "version": "0.1.0",
  "description": "Composable Node.js ESM '--experimental-loader's",
  "author": "Concision <contact@concision.me>",
  "license": "MIT",
  "repository": "https://github.com/concision/esm-loaders.git",
  "private": true,
  "workspaces": [
    "loader-types",
    "chain-polyfill",
    "http",
    "typescript",
    "coffescript"
  ],
  "scripts": {
    "clean": "yarn workspaces foreach run clean",
    "lint": "yarn workspaces foreach run clean",
    "build": "yarn workspaces foreach run build",
    "prepack": "yarn workspaces foreach run prepack"
  },
  "devDependencies": {
    "@types/node": "^14.14.6",
    "ts-node": "git+https://github.com/TypeStrong/ts-node.git",
    "typescript": "^4.0.5"
  }
}
